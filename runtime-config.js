"use strict";

// Deployment automation replaces this object only in the AWS-hosted artifact.
// The repository and GitHub Pages remain the scripted, no-backend showcase.
window.FOLDERHOME_LIVE_DEMO = Object.freeze({
  enabled: false,
  apiBaseUrl: "",
  apiKey: "",
});
