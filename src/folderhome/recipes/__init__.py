"""Packaged capability recipes: declarative journeys over existing endpoints.

The JSON files here ship inside the wheel so a recipe is available to an
installed FolderHome, not only to a repository checkout. A recipe grants no new
capability; it only names existing endpoints, their complete requests and the
logical resources they share.
"""
